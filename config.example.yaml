clusters:
  - name: local
    bootstrap: localhost:9092
    # sasl: { mechanism: SCRAM-SHA-512, username: app, password: "${KAFKA_PASSWORD}", tls: true }
  # A second cluster is just another list entry:
  # - name: staging
  #   bootstrap: localhost:9093
    # schema_registry: { url: http://localhost:8081 }
    # Diagnostic: report what Arne asked this cluster's brokers, per broker and
    # per API, at /api/clusters/<name>/broker-calls. 0 (default) = off.
    # broker_call_stats_ms: 2000
server: { port: 8080 }
limits: { sampler_interval_secs: 10, timeline_scan_budget: 250000 }
